---
assignment: hw20

compilers:
  - &cpp 'g++ --std=c++11 $@ -o $@.exec'

files:
  - file: hw20.txt
  - file: Food1.cpp
    commands: *cpp
    tests: $@.exec
  - file: Food2.cpp
    commands: *cpp
    tests: $@.exec
  - file: Food3.cpp
    commands: *cpp
    tests: $@.exec
  - file: Veggie2.cpp
    commands: *cpp
    tests: $@.exec
  - file: Veggie3.cpp
    commands: *cpp
    tests: $@.exec
  - file: Veggie4.cpp
    commands: *cpp
    tests: $@.exec
  - file: Veggie5.cpp
    commands: *cpp
    tests: $@.exec
  - file: Veggie6.cpp
    commands: *cpp
    tests: $@.exec
