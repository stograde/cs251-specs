---
assignment: hw13
# possibly compile to their directory, and do a symlink?
# possibly copy over all the .yaml?
# possibly start the server from each directory

compilers:
  - &cpp 'g++ --std=c++11 $@ -o $@.exec ./react.o -lcurl'
  - &server 'g++ --std=c++11 $@ -o ../../../server/server_file ./react.o -lcurl'
  - &server2 'g++ --std=c++11 $@ -o ../../a.out ./react.o -lcurl'
  - &copy 'cp *.yaml ../../../server/home.yaml'

files:
  - [ hw13.txt ]
  - [ screens.cpp, *copy, *server, web: true]
  - [ machinery.cpp, *cpp ]
  - [ pizza.cpp, *cpp ]
  - [ home.yaml ]
  - [ screen1.yaml ]

tests:
  # - [ screens.cpp.exec, ../../../webapp/server.py ]
  - [ screens.cpp, $@.exec ]
  - [ machinery.cpp, $@.exec ]
  - [ pizza.cpp, $@.exec ]

inputs:
  - [ react.o ]
  - [ react.h, ../react.h ]
  - [ print.h ]
  - [ print.o ]