---
assignment: hw12

compilers:
  - &cpp 'g++ --std=c++11 $@ -o $@.exec'

files:
  - [ hw12_questions.txt ]
  - [ swapPtr.cpp, *cpp ]
  - [ myprog.cpp, *cpp ]
  - [ loadFile.cpp, *cpp ]
  - [ avg3.cpp, *cpp ]

tests:
  - [ swapPtr.cpp, $@.exec ]
  - [ myprog.cpp, $@.exec < 3 5 ]
  - - loadFile.cpp
    - $@.exec < firefox.txt
    - $@.exec < tiny.txt
  - - avg3.cpp
    - $@.exec < firefox.txt
    - $@.exec < tiny.txt

inputs:
  - firefox.txt
  - tiny.txt