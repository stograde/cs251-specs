---
assignment: hw10

compilers:
  - &cpp 'g++ --std=c++11 $@ -o $@.exec'

files:
  - file: hw10.txt
  - file: steps.cpp
    commands: *cpp
  - file: ptrptr.cpp
    commands: *cpp
  - file: sizeof.cpp
    commands: *cpp
  - file: length_of.cpp
    commands: *cpp
