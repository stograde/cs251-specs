---
assignment: hw10

compilers:
  - &server 'g++ -std=c++11 $@ $SERVER'
  - &cpp 'g++ --std=c++11 $@ -o $@.exec'

files:
  - [ length_of2.cpp, *server, web: true ]
  - [ write_at2.cpp, *server, web: true ]
  - [ append2.cpp, *server, web: true ]
  - [ is_match.cpp, *server, web: true ]
  - [ terminal.cpp, *cpp ]

tests:
  - [ terminal.cpp, $@.exec ]

inputs:
  - sd_fun.h
  - sd_fun.o
  - str_util.o
