---
assignment: lab2

compilers:
  - &server 'g++ -std=c++11 $@ $SERVER'
  - &dataport 'g++ --std=c++11 data_port.o $@ $SERVER -lcurl'

files:
  - [ chat1b.cpp, *server, web: true ]
  - [ chat2b.cpp, *server, web: true ]
  - [ chat3b.cpp, *server, web: true ]
  - [ chat4b.cpp, *server, web: true ]
  - [ chat5b.cpp, *server, web: true, optional: true ]
  - [ chat6b.cpp, *dataport, web: true ]

inputs:
  - sd_fun.h
  - data_port.o