---
assignment: hw7

compilers:
  - &server 'g++ -std=c++11 $@ $SERVER'
  - &dataport 'g++ --std=c++11 data_port.o $@ $SERVER -lcurl'

files:
  - [ hw7_questions.txt ]


  - [ simple_button.cpp, *server, web: true ]

inputs:
  - prelude
  - postlude
  - sd_fun.h
