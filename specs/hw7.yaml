---
assignment: hw7

compilers:
  - &cpp 'g++ --std=c++11 $@ -o $@.exec'
  - &server 'g++ -std=c++11 $@ $SERVER'
  - &dataport 'g++ --std=c++11 data_port.o $@ $SERVER -lcurl'

files:
  - [ hw7_questions.txt ]
  - [ power_table.cpp, *server, web: true ]

inputs:
  - prelude
  - postlude
  - sd_fun.h
