---
assignment: hw6

compilers:
  - &server 'g++ -std=c++11 $@ $SERVER'

files:
  - [ hw6.txt ]
  - [ text_input6.cpp, *server, web: true ]
  - [ countdown2.cpp, *server, web: true ]
  - [ countdown3.cpp, *server, web: true ]
  - [ sequences.cpp, *server, web: true ]

input_files:
  - prelude
  - postlude
