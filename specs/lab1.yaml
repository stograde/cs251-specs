---
assignment: lab1

compilers:
  - &server 'g++ --std=c++11 $@ $SERVER'

files:
  - [ chat1.cpp, *server, web: true ]
  - [ chat2.cpp, *server, web: true ]
  - [ chat3.cpp, *server, web: true ]
  - [ data2.cpp, *server, web: true ]
  - [ data3.cpp, *server, web: true ]
  - [ chat4.cpp, *server, web: true ]

inputs:
  - prelude
  - postlude
