---
assignment: hw1

compilers:
  - &cpp 'g++ --std=c++11 $@ -o $@.exec'
  - &server 'g++ --std=c++11 $@ $SERVER'

files:
  - [ hello.cpp, *server, web: true ]
  - [ hw1_questions.txt ]
  - [ Makefile ]
  - [ Dog.h ]
  - [ Dog.cpp ]
  - - tryDog.cpp
    - rm -f tryDog tryDog.o Dog.o
    - make

tests:
  - [ tryDog.cpp, ./tryDog ]

inputs:
  - prelude
  - postlude
  - sd_fun.h