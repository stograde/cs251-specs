---
assignment: hw8

compilers:
  - &cpp 'g++ --std=c++11 $@ -o $@.exec'
  - &server 'g++ -std=c++11 $@ $SERVER'

files:
  - [ hw8_questions.txt ]
  - [ array_1.cpp, *server ]
  - [ array_2.cpp, *server ]
  - [ power_table_2.cpp, *server ]
  - [ dec_array.cpp, *server ]

inputs:
  - prelude
  - postlude
